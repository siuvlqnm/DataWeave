import SwiftUI

struct AutomationView: View {
    var body: some View {
        NavigationView {
            Text("自动化视图")
                .navigationTitle("自动化")
        }
    }
}

#Preview {
    AutomationView()
} 